name: gradle_mate
description: "أداة تساعد مطوري Android على اختيار الإصدارات المتوافقة من Gradle, AGP, JDK, وAndroid Studio."

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_screenutil: ^5.9.3
  http: ^1.4.0
  provider: ^6.1.5
  go_router: ^8.0.0
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
